# `/admin/companies`

Source: `src/app/(app)/admin/companies/page.tsx:5`

Platform-admin queue of pending companies. `CompanyApprovals` lets an admin approve or decline each request; status updates record reviewer/time and revalidate the page.
