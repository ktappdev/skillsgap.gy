# `/provider/setup`

Source: `src/app/provider/setup/page.tsx:9`

Requires sign-in and redirects existing provider owners to `/provider`. New users submit provider name/location plus optional phone, HTTPS contact URL, and description. The server always creates an unverified provider and redirects to `/provider`.

Back: logo → `/`; no explicit cancel action.
