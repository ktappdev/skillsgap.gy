# `/auth/error`

Source: `src/app/auth/error/page.tsx:7`

Displays either an expired reset-link state or a generic invalid confirmation-link state. Provides a primary recovery/sign-in action and a secondary `Go home` action. It is public and static apart from the `reason` query parameter.
