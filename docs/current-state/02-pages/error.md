# Global error state

Source: `src/app/error.tsx:10`

Client error boundary with `Try again` through Next reset and `Go home` → `/`. It describes a temporary pathway-loading issue. It has no account-aware fallback.
