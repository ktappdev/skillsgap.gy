# `/signup/provider`

Source: `src/app/(auth)/signup/provider/page.tsx:15`; UI: `src/components/auth/auth-form.tsx`

Uses the reusable signup form with provider audience copy and provider-safe continuation rules. After account creation, provider onboarding continues at `/provider/setup`.

Back: logo → `/`.
