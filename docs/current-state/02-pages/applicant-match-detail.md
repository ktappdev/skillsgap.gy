# `/matches/[matchId]`

Source: `src/app/(app)/matches/[matchId]/page.tsx:12`

Applicant-only current match detail, with demo fallback support. Shows score/threshold progress, strengths, prioritized gaps, role share, role-specific profile consent, and threshold-gated apply/withdraw. Each gap can expose training/course/share/plan actions.

Back: explicit `← Back to my pathway` → `/dashboard`.
