# docs/ui-tweaks

Working notes for taking the "AI made this" edge off the public-facing experience. Not committed to git — keep it local and refer back to it as we iterate.

Files:

- `README.md` — this index
- `ai-smell-audit.md` — the patterns making it read as AI-generated right now
- `surface-tweaks.md` — concrete, page-by-page diffs we'd apply
- `voice-and-tone.md` — copy rules to keep it from sinking back

## Git-ignore

The parent `.gitignore` covers `docs/ui-tweaks` via the personal-notes pattern. If it ever shows up in `git status`, add:

```
docs/ui-tweaks/
```
