import Link from "next/link";
import { ManagementNav } from "@/components/skillsgap/management-nav";
import { requireApprovedCompanyMember } from "@/lib/auth/queries";
export default async function CompanyPage() { const { supabase, companyId } = await requireApprovedCompanyMember(); const [{ count: roleCount }, { count: candidateCount }, { count: slotCount }] = await Promise.all([supabase.from("job_roles").select("id", { count: "exact", head: true }).eq("company_id", companyId).eq("status", "active"), supabase.from("job_matches").select("id,job_roles!inner(company_id)", { count: "exact", head: true }).eq("job_roles.company_id", companyId).eq("status", "current"), supabase.from("interview_slots").select("id,job_fairs!inner(company_id)", { count: "exact", head: true }).eq("job_fairs.company_id", companyId)]); return <div className="mx-auto max-w-6xl px-4 py-8 sm:px-6 lg:px-8"><h1 className="text-3xl font-semibold tracking-tight">Company workspace</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-muted">Candidates stay anonymous until they share their profile or confirm an interview.</p><div className="mt-6"><ManagementNav area="company" /></div><dl className="mt-6 grid gap-4 sm:grid-cols-3"><Metric label="Active roles" value={String(roleCount ?? 0)} /><Metric label="Matched candidates" value={String(candidateCount ?? 0)} /><Metric label="Open slots" value={String(slotCount ?? 0)} /></dl><section className="mt-6 grid gap-4 lg:grid-cols-2"><article className="rounded-lg border border-border bg-surface p-6"><h2 className="text-xl font-semibold">Review your closest matches</h2><p className="mt-2 text-sm leading-6 text-muted">Anonymized skills, scores, and outstanding gaps for your active roles.</p><Link href="/company/candidates" className="mt-5 inline-flex min-h-11 items-center text-sm font-semibold text-accent underline-offset-4 hover:underline">View candidates →</Link></article><article className="rounded-lg border border-border bg-surface p-6"><h2 className="text-xl font-semibold">Identity stays with the applicant</h2><p className="mt-2 text-sm leading-6 text-muted">Names, contact details, and CVs appear only after the applicant gives consent for your role.</p></article></section></div>; }
function Metric({ label, value }: { label: string; value: string }) { return <div className="rounded-lg border border-border bg-surface p-5"><dt className="text-sm text-muted">{label}</dt><dd className="mt-2 text-3xl font-semibold tracking-tight text-foreground">{value}</dd></div>; }
