import Link from "next/link";
import { notFound } from "next/navigation";

import { isDemoApplicantMetadata } from "@/lib/auth/demo";
import { requireApplicant } from "@/lib/auth/queries";
import { getDemoMatch } from "@/lib/skillsgap-demo";
import { getApplicantMatch } from "@/lib/skillsgap/queries";
import { ApplyButton } from "@/components/skillsgap/apply-button";
import { StatusPill } from "@/components/skillsgap/milestone-path";
import { ShareProfileButton } from "@/components/skillsgap/share-profile-button";
import { TrainingPlanButton } from "@/components/skillsgap/training-plan-button";

export default async function MatchDetailPage({ params }: { params: Promise<{ matchId: string }> }) {
  const { matchId } = await params;
  const { supabase, user } = await requireApplicant();
  const liveMatch = await getApplicantMatch(supabase, user.id, matchId);
  const match = liveMatch ?? getDemoMatch(matchId, isDemoApplicantMetadata(user.user_metadata));
  if (!match) notFound();
  const remaining = Math.max(match.threshold - match.score, 0);
  return <div className="mx-auto max-w-4xl px-4 py-8 sm:px-6 lg:px-8"><Link href="/dashboard" className="text-sm font-semibold text-accent underline-offset-4 hover:underline">← Back to my pathway</Link><header className="mt-7 border-b border-border pb-7"><p className="text-xs font-bold uppercase tracking-[0.18em] text-accent">{match.company}</p><p className="mt-2 text-xs font-semibold text-accent">{match.isDemo ? "Curated demo pathway" : "Company-published role"}</p><div className="mt-3 flex flex-wrap items-start justify-between gap-4"><div><h1 className="text-3xl font-semibold tracking-[-0.04em] text-foreground sm:text-4xl">{match.title}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-muted">Your route is based on the experience in your profile—not a guess at what role you should choose.</p></div><StatusPill tone="accent">{match.score}% match</StatusPill></div></header>{liveMatch ? <div className="mt-6 space-y-4"><ShareProfileButton roleId={match.roleId} alreadyShared={match.consented} /><ApplyButton roleId={match.roleId} initialStatus={match.applicationStatus} eligible={match.score >= 85} /></div> : null}<section className="mt-8 grid gap-5 md:grid-cols-[.9fr_1.1fr]"><div className="border border-border bg-surface p-5 shadow-sm"><p className="text-xs font-bold uppercase tracking-[0.15em] text-accent">Your next milestone</p><h2 className="mt-2 text-xl font-semibold tracking-tight">{remaining}% to interview eligibility</h2><div className="mt-5 h-3 overflow-hidden rounded-full bg-surface-muted"><div className="h-full bg-accent" style={{ width: `${match.score}%` }} /></div><p className="mt-4 text-sm leading-6 text-muted">The interview threshold is {match.threshold}%. Every mandatory requirement must also be confirmed.</p></div><div className="border border-border bg-surface p-5 shadow-sm"><p className="text-xs font-bold uppercase tracking-[0.15em] text-accent">What already counts</p><h2 className="mt-2 text-xl font-semibold tracking-tight">Strengths we recognized</h2>{match.strengths.length > 0 ? <ul className="mt-4 space-y-3" role="list">{match.strengths.map((strength) => <li key={strength} className="flex gap-3 text-sm"><span className="grid size-5 place-items-center rounded-full bg-accent text-xs font-bold text-white" aria-hidden="true">✓</span><span>{strength}</span></li>)}</ul> : <p className="mt-4 text-sm leading-6 text-muted">No strengths are confirmed yet. Review the possible CV translations and choose the ones that describe your experience.</p>}</div></section><section className="mt-8 border border-border bg-surface p-5 shadow-sm sm:p-7" aria-labelledby="gaps-heading"><p className="text-xs font-bold uppercase tracking-[0.15em] text-accent">A practical route forward</p><h2 id="gaps-heading" className="mt-2 text-2xl font-semibold tracking-tight">{match.gaps.length} requirements left</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-muted">These are the specific gaps for this role. Start with one; it will update your route when the qualification is confirmed.</p><div className="mt-6 divide-y divide-border border-y border-border">{match.gaps.map((gap) => <div key={gap.name} className="py-5 first:pt-0 last:pb-0"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-sm font-semibold text-foreground">{gap.name}</p><p className="mt-1 text-sm text-muted">{gap.type}</p></div><StatusPill>Required</StatusPill></div>{gap.training ? <div className="mt-4 flex flex-col gap-3 bg-surface-muted p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-semibold text-foreground">{gap.training}</p><p className="mt-1 text-sm text-muted">Ask the provider to confirm current intake, cost, and eligibility.</p>{gap.trainingUrl?.startsWith("https://") ? <a href={gap.trainingUrl} target="_blank" rel="noreferrer" className="mt-2 inline-flex text-sm font-semibold text-accent underline-offset-4 hover:underline">Open official provider site <span aria-hidden="true">↗</span></a> : null}</div><TrainingPlanButton qualification={gap.name} gapId={gap.id} initialStatus={gap.status} /></div> : <p className="mt-4 text-sm leading-6 text-muted">No verified local program listed yet. This does not mean the qualification is unavailable.</p>}</div>)}</div></section></div>;
}
