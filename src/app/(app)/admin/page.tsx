import Link from "next/link";
import { ManagementNav } from "@/components/skillsgap/management-nav";
import { requirePlatformAdmin } from "@/lib/auth/queries";
export default async function AdminPage() { await requirePlatformAdmin(); return <div className="mx-auto max-w-6xl px-4 py-8 sm:px-6 lg:px-8"><p className="text-xs font-bold uppercase tracking-[0.18em] text-accent">Platform administration</p><h1 className="mt-3 text-3xl font-semibold tracking-[-0.04em]">Keep the ecosystem credible.</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-muted">Manage verified companies, training providers, and the career guidance behind every public pathway. Demo records are clearly labelled until independently verified.</p><div className="mt-7"><ManagementNav area="admin" /></div><div className="mt-8 grid gap-4 md:grid-cols-4"><AdminCard title="Company requests" value="Live" href="/admin/companies" /><AdminCard title="Qualifications" value="Live" href="/admin/qualifications" /><AdminCard title="Training programs" value="Live" href="/admin/training" /><AdminCard title="Career guidance" value="Live" href="/admin/career-guidance" /></div></div>; }
function AdminCard({ title, value, href }: { title: string; value: string; href: string }) { return <Link href={href} className="border border-border bg-surface p-5 shadow-sm transition hover:border-accent"><p className="text-sm text-muted">{title}</p><p className="mt-2 text-3xl font-semibold tracking-tight">{value}</p><p className="mt-5 text-sm font-semibold text-accent">Manage →</p></Link>; }
