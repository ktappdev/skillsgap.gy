import Link from "next/link";
import { DemoResetCard } from "@/components/skillsgap/demo-reset-card";
import { ManagementNav } from "@/components/skillsgap/management-nav";
import { requirePlatformAdmin } from "@/lib/auth/queries";
export default async function AdminPage() { await requirePlatformAdmin("/admin"); return <div className="mx-auto max-w-6xl px-4 py-8 sm:px-6 lg:px-8"><h1 className="text-3xl font-semibold tracking-tight">Platform administration</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-muted">Verify companies and training providers, and keep the guidance behind every public pathway current. Demo records stay labelled until independently verified.</p><div className="mt-6"><ManagementNav area="admin" /></div><DemoResetCard /><nav className="mt-6 grid gap-4 md:grid-cols-4" aria-label="Administration sections"><AdminCard title="Company requests" description="Review pending verifications" href="/admin/companies" /><AdminCard title="Qualifications" description="Maintain the taxonomy" href="/admin/qualifications" /><AdminCard title="Training programs" description="Check providers and outcomes" href="/admin/training" /><AdminCard title="Career guidance" description="Edit the public catalogue" href="/admin/career-guidance" /></nav></div>; }
function AdminCard({ title, description, href }: { title: string; description: string; href: string }) { return <Link href={href} className="rounded-lg border border-border bg-surface p-5 transition hover:border-accent"><p className="font-semibold text-foreground">{title}</p><p className="mt-1 text-sm text-muted">{description}</p><p className="mt-5 text-sm font-semibold text-accent">Manage →</p></Link>; }
